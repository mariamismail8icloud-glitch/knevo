spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/knevo}
    username: ${DB_USER:knevo}
    password: ${DB_PASSWORD:knevo}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
