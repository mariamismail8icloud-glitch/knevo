disabled_rules:
  - trailing_whitespace
  - line_length

opt_in_rules:
  - empty_count
  - explicit_init
  - first_where

identifier_name:
  excluded:
    - id
    - ok

included:
  - Sources
  - Tests

excluded:
  - .build

reporter: "xcode"
